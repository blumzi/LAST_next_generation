#!/bin/bash

trap die SIGINT

function die() {
    pkill -9 -g ${pid}
}

cd /home/ocs/python/LAST_next_generation
source .venv/bin/activate
python3 unit/unit-server.py >/dev/null 2>&1 &
pid=$!
wait -n
